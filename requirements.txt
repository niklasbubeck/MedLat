# The canonical dependency list lives in pyproject.toml.
# This file is kept so that legacy `pip install -r requirements.txt`
# workflows still work — it installs the package and its core deps.
#
# For development / tooling:    pip install -e ".[dev]"
# For the full feature matrix:   pip install -e ".[all]"
-e .
